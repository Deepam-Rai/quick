pip3 install -e

#this script is to install this package.
# make sure that it has execuion permission. [ chmod +x install.sh]
# if this does not run then user can simply run following equivalent commands
#  $ pip3 install -e
#  -e is for being editable part. so that when files are edited u need not install again.
